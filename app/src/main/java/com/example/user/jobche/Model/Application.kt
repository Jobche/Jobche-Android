package com.example.user.jobche.Model

data class Application(val id: Int,
                       val applicantId: Int,
                       val taskId: Int,
                       val accepted: Boolean)